using UnityEngine;

// Cannot damage environment
public class Cir_1 : BaseEnemy
{
    //Vector2 moveDir;

    //private void Start()
    //{
    //    Setup(getMaxHP());
    //    moveDir = Constants.DIAGONAL_VECTORS[Random.Range(0, 4)];
    //    transform.GetComponent<Rigidbody2D>().AddForce(moveDir * moveSpeed * 100);
    //}

    //private void OnTriggerEnter2D(Collider2D col)
    //{
    //    if (col.GetComponent<Damageable>() != null && col.tag == "Player")
    //    {
    //        col.gameObject.GetComponent<PlayerHealth>().DoDamage(attackDmg);
    //    }
    //}
}
